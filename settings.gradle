pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'org.ajoberstar.grgit' version "${grgitPluginVersion}"
    }
}

rootProject.name = 'common-tree'
